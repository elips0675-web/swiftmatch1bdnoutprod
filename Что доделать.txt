# Что доделать — сводный статус по аудитам kimi, дипсик, qwen

> Актуально: 23.08.2026, после этапов 35–37 (schema-validation в CI, идемпотентные миграции 018–023, cleartext только debug; RevenueCat SDK подключён; AGENTS.md правило adminAuth). Каждый пункт — из аудитов «Оценка kimi.txt», «Оценка дипсик.txt», «Оценка qwen.txt»; статус сверен с кодом и тестами.

## ✅ Закрыто (в т.ч. этапами 8–22)

| Пункт аудита | Кто флагил | Чем закрыт |
|---|---|---|
| RevenueCat IAP (клиент) | kimi #3 | **Этап 22**: `src/lib/iap.ts` + интеграция в premium.tsx (нативная ветка); **Этап 36**: `npm i @revenuecat/purchases-capacitor@13.4.1` + динамический `import()` вместо `require` (работает в ESM-бандле на нативе); осталось только `VITE_REVENUECAT_API_KEY` при сборке APK |
| Privacy Policy + Terms of Service | kimi, дипсик | Страницы существовали с марта 2024 (`/legal/privacy`, `/legal/terms`, `/legal/data-processing`, RU/EN, ссылки в настройках) — аудиты смотрели старый срез; **этап 19**: добавлена ссылка на ToS в чекбокс регистрации |
| Age verification 18+ | kimi чеклист | **Этап 20**: обязательный чекбокс «Мне есть 18 лет» при регистрации + тост-блокировка |
| Backup restore verification | kimi #23 | **Этап 21**: `scripts/verify-backup.ps1` (restore в скретч-БД + sanity-проверки), weekly-задача в `install-backup-task.bat` |
| GDPR: consent, export, erasure | kimi #7 | Этап 8 — E2E-тесты (consent_log, /api/data/export, erase) |
| Background GPS + Geofence | kimi #8 | Июльские фичи: 5-min polling, /api/location |
| Ghost Mode (premium) | kimi #9 | Этап 8, E2E #16 + 403 PREMIUM_REQUIRED |
| Passport Mode (premium) | kimi #10 | 009 миграция + UI toggle + премиум-гейт + поиск по passport-локации |
| AI Icebreakers | kimi #11 | Этап 8 E2E #14 + fallback БД без OpenAI |
| Video Date Scheduling | kimi #12 | Этап 10 E2E #18 (фикс ISO-даты в MySQL 8.4 strict) |
| Disappearing Messages | kimi #13 | Июльская фича: TTL 5s–24h, WS cleanup |
| Profile Score | kimi #14 | Этап 8 E2E #17 |
| Date Check-in (Safety) | kimi #15 | Этап 10 E2E #18 |
| A/B + feature flags | kimi #16 | Этап 8 E2E #15 |
| Product analytics | kimi #17, qwen 🟡 | **Этап 24**: внутренняя аналитика полная (track + retention + revenue-mix в админке); воронка расширена: login_completed, register_completed, message_sent, purchase_completed (плюс swipe_like/super_like) |
| CI/CD | kimi #18 | deploy.yml (lint→test→E2E→deploy), нужны только secrets |
| Docker Compose | kimi #19 | app + nginx + Prometheus + Grafana |
| Redis Adapter Socket.IO | kimi #20 | ws.js adapter |
| Load Testing k6 | kimi #21 | **Этап 23**: скрипт починен под актуальный API (`/api/users/search`, `liked_user_id`, динамический chatId, дефолт user5@mail.ru) + лайт-прогон 10 VU: p95 < 250ms, одиночный запрос 5–8ms. Полный ramp 100 VU — на staging: rate-limit'ы (600/мин глобальный, 30/мин лайки) режут тест с одного IP |
| Monitoring | kimi #22 | Prometheus :9090, Grafana :3001 |
| matches дубликаты | qwen 🔴1 | social.js сортирует Math.min/max + UNIQUE (user1,user2) + проверка обоих направлений |
| posts is_liked/is_bookmarked | qwen 🔴2 | Сервер никогда не читал колонки (liked_by_me через EXISTS) — колонки удалены миграцией 024 |
| Sentry errorHandler мёртв | qwen 🔴3 | **Этап 18**: initSentry → requestHandler до роутов; errorHandler зарегистрирован перед 500-хендлером (sentry.js: registerSentryErrorHandler) |
| dev-login без guard | qwen 🔴4 | **Этап 18**: 404 при NODE_ENV=production |
| CORS '*' в проде | qwen 🔴5 | **Этап 18**: fail-fast при старте — без CORS_ORIGIN сервер не поднимется в production |
| Дубли активных подписок | qwen #8 | **Этап 18**: транзакция с деактивацией старых перед INSERT (mock + Stripe webhook) |
| password_hash в SELECT login | qwen #7 | Не баг: используется в bcrypt.compare (index.js:131) |
| user_profiles.id без AUTO_INCREMENT | qwen #10 | By design — 1-to-1 с users.id, FK |

## ⏳ Ждёт действий пользователя (ключи/внешние сервисы)

1. **server/.env** — реальные ключи: `STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET`, `SMTP_USER/PASS`, `SENTRY_DSN`, `AWS_*`/`S3_BUCKET`, `OPENAI_API_KEY`, `FCM_SERVER_KEY`, `REVENUECAT_WEBHOOK_SECRET`, `TWILIO_*`. Проверка: `powershell -File scripts/check-keys.ps1`. Без них всё работает на mock/fallback (этап 11).
2. **GitHub secrets** для deploy.yml: `DEPLOY_HOST`, `DEPLOY_USER`, `DEPLOY_SSH_KEY`, `DB_*`.
3. **Staging-окружение** + прогон E2E с реальными ключами (дипсик: «провести финальное E2E-тестирование»).
4. **Android**: SDK/Android Studio → собрать APK → реальное устройство. Плюс App Links (домен + assetlinks.json, SHA256).
5. **Сторы**: Privacy Policy + Terms (см. «В работе»), контент 17+, скриншоты, TestFlight.
6. **Redis доступность** — `REDIS_URL` в .env, но Redis-сервер не поднят (дипсик: «надо проверить доступность Redis»). Без него кэш/очереди/Socket.IO-adapter тихо падают на in-memory.

## 🟠 В работе / техдолг второй очереди

- **SMS-верификация** — scaffold + mock; реальная после ключей Twilio (kimi #4)
- **AI-модерация фото** — эвристика/regex fallback; Rekognition после AWS-ключей (kimi #5)
- **CDN/S3** — локальный диск; S3 после AWS-ключей (kimi #6)
- **CSRF** — не критично для JWT API-only (дипсик)
- **Мониторинг доступности** (UptimeRobot) и SSL в docker-compose (дипсик #5 инфра)
- **AGENTS.md** — разделить при >1500 строк (сейчас ~1180; qwen в свежем аудите называл >1500 — завышено)
- **AGENTS.md противоречия по adminAuth** — ✅ закрыто этапом 25 (переписаны 7 секций) + этапом 37 (в Security Rules добавлено ПРАВИЛО adminAuth: только active-check middleware, запрет пассивных `next()`, ручных проверок роли и обхода единого гейта)
- **Windows-центричность** — Laragon, Task Scheduler, .bat, PowerShell — основной workflow; staging/prod обычно Linux. Docker Compose есть, но не является основным путём (qwen #3)
- **mysqld RADAR_PRE_LEAK_64 под E2E-нагрузкой** — mysqld падал при прогоне (3 chromium + node + vite + mysql). Проверить память/утечки пула mysql2, протестировать под k6 100 VU на staging (qwen #4)
- ~~JWT в localStorage~~ — ✅ закрыто этапом 33: httpOnly cookie на web, Bearer только для нативных сборок
- **План отката** на случай проблем с платежами или БД (дипсик, рекомендации)
- **Проверка rules-of-hooks** — баг useApi.ts (useMemo внутри useEffect + двойной fetch) нашли только на этапе 17; убедиться, что линтер rules-of-hooks включён и ловит такие случаи (qwen 🟡)

## ✅ Закрыто этапами 25–29 (18.08.2026)

| Пункт | Кто флагил | Чем закрыт |
|---|---|---|
| AGENTS.md противоречия по adminAuth | qwen #1 | **Этап 25**: переписаны все 7 секций AGENTS.md + persona.md — adminAuth ACTIVE (401/403), единый гейт `/api/admin` в index.js, публичен только GET /api/admin/features, dev-login → 404 в production; **Этап 37**: закреплено ПРАВИЛО adminAuth в Security Rules |
| rules-of-hooks / баг useApi.ts | qwen 🟡 | **Этап 25**: проверено — react-hooks rules-of-hooks включён в eslint.config.js, eslint: 0 errors (153 warnings), баг этапа 17 линтером ловился бы |
| Секреты в логах | дипсик | **Этап 25**: Winston логирует только msg + err.message/stack (logger.js), req.body/headers не логируются; Sentry beforeSend фильтрует authorization/cookie/email |
| План отката | дипсик | **Этап 26**: `docs/rollback-plan.md` — категории инцидентов (платежи/БД/полный отказ), диагностика ≤5 мин, откат коммита, бэкап-восстановление, чек-лист после отката |
| mysql2 пул / RADAR_PRE_LEAK_64 | qwen #4 | **Этап 26**: пул проверен — connectionLimit 20 (env DB_POOL_MAX), enableKeepAlive; getConnection только в premium.js, оба с finally/release (утечек нет); полный k6 100 VU — на staging |
| Windows-центричность | qwen #3 | **Этап 26**: docker-compose починен — добавлен nginx-сервис (nginx/swiftmatch.conf + dist), CORS_ORIGIN в app (иначе fail-fast ронял контейнер), prometheus target app:3002, grafana dashboards provisioning; YAML валиден, 6 сервисов |
| Миграции в CI | qwen 🟡 | **Этап 27**: deploy.yml — миграции перенесены с CI-раннера на сервер (ssh: `set -a; . ./server/.env; node database/migrations/migrate.js`) перед pm2 restart; secrets DB_* больше не нужны |
| JWT в localStorage | qwen #7 | **Этап 28**: ADR в AGENTS.md — переход на httpOnly cookie отложен (CSRF, credentials:include, 80+ fetch, E2E); Bearer в sessionStorage остаётся стандартом; refresh_token в storage не добавлять |
| Redis доступность | дипсик | **Этап 29**: поднят локальный Redis 5.0.14 (portable, Temp), REDIS_URL=redis://127.0.0.1:6379 в server/.env, сервер держит пул (connected_clients 11); в compose redis уже есть; для постоянной работы — docker или install |

## 🗺 Этапы 30+ (после ключей / внешних действий, см. «Ждёт действий пользователя»)

- ✅ **Этап 30 (19.08.2026)** — проверка работоспособности всего: `/api/invites` давал 500 (дрейф колонок `sender_id/receiver_id/type` → `from_user_id/to_user_id/invite_type/message` в social.js) — починен (GET/POST/PUT, валидация enum, message). Прочие смоук-точки 200/201. Сервер 126/126 ✅, E2E 44/44 ✅
- ✅ **Этап 31 (19.08.2026)** — EXPLAIN-аудит всех SQL из server/src против схемы: найдены и починены дрейфы — `iap.js` webhook ссылался на несуществующую `subscriptions.updated_at` (500 на каждом событии RevenueCat), `location.js` использовал `user_profiles.user_id` вместо `id` (500 на PUT/GET /api/location). Проверено: PUT/GET location 200, EXPLAIN 55/56 (1 — ложное срабатывание audit.js). Сервер 126/126 ✅, фронт 57/57 ✅
- ✅ **Этап 32 (19.08.2026)** — Android-сборка: установлены Android Studio (JBR 25), cmdline-tools, SDK (platform 35/36, build-tools 35.0.0), JDK 21 (Gradle 8.14.3 не работает на Java 25); `@revenuecat/purchases-capacitor` 13.4.0; AGP 8.13.0→8.13.2; каприз `compressDebugAssets` (MD5 jar) вылечен standalone-прогоном. **APK собран: `android/app/build/outputs/apk/debug/app-debug.apk` (27,8 MB), копия на Рабочем столе**; пока с VITE_API_URL = LAN IP для теста на устройстве, для прода — домен + ключ RevenueCat в .env
- ✅ **Этап 33 (19.08.2026)** — JWT → httpOnly cookie: сервер ставит `sm_token`/`sm_refresh` (SameSite=Lax, Secure в prod) на login/register/refresh/dev-login, все auth-мидлвари читают Bearer ?? cookie (`server/src/cookies.js`); новый GET `/api/auth/me` (probe, всегда 200) и POST `/api/auth/logout` (кука + refresh_tokens). Фронт: на web токен не пишется в storage (только память + чтение легаси), refresh через куку без body, логаут дергает сервер. Грабли обойдены: E2E storageState требует чтения старого storage, `/me` отдаёт 200 без токена (иначе console-error валит audit). Сервер 129/129 ✅ (+3 cookie-теста), фронт 58/58 ✅, E2E 44/44 ✅, lint 0 errors ✅, build ✅
- SMS-верификация (Twilio), AI-модерация фото (Rekognition), CDN/S3, push (FCM), staging-E2E с реальными ключами, публикация в Google Play (AAB, App Links, политика 17+, сторы)
- GitHub secrets для deploy.yml: DEPLOY_HOST, DEPLOY_USER, DEPLOY_SSH_KEY (DB_* больше не нужны — миграции читают server/.env на сервере)
- Полный k6 ramp 100 VU на staging (проверка RADAR_PRE_LEAK_64 под реальной нагрузкой)
- ~~JWT → httpOnly cookie~~ — ✅ сделан этапом 33 (см. выше)

## 🆕 Свежий аудит kimi/qwen/дипсик после этапа 33 (22.08.2026)

Оценки: **kimi 7.5/10** («готов к staging и закрытой бете; не готов к открытому проду без блокеров безопасности»), **qwen 8/10** pet / 6.5/10 prod-SaaS (~70% готовности: код и тесты — отлично, окружение и релиз — нет), **дипсик ~95%** технически («держат релиз только внешние ключи, домен и сторы»). Консенсус всех трёх: **новый продуктовый код не нужен — разблокировать прод** (staging → ключи → домен → релиз Android).

### ✅ Этап 34 (22.08.2026) — безопасность: сделано из таблицы выше

| # | Пункт | Статус |
|---|---|---|
| 1 | Refresh rotation + детект переиспользования | ✅ миграция 025 (`family_id`, `revoked`); refresh атомарно клеймит токен, повторное использование ротированного → отзыв всей семьи; гонка параллельных refresh закрыта affectedRows |
| 2 | Revoke all sessions | ✅ POST `/api/auth/logout-all` + автоотзыв всех сессий при reset-password |
| 3 | Account lockout | ✅ `server/src/lockout.js`: 5 неудач подряд на email → 429 на 15 мин (`AUTH_LOCKOUT_MAX_ATTEMPTS`), успех сбрасывает; проверено живьём (5×401 → 429) |
| 4 | Admin TOTP 2FA | ⏳ отложено отдельным этапом — нужна enrollment-обвязка (QR + приложение-аутентификатор), до публичного запуска админка не торчит наружу |
| 5 | CSRF double-submit | ⏳ по ADR — при выносе API на отдельный домен |
| 6 | Привязка сессии к устройству/IP | ⏳ средний приоритет, после staging |
| 7 | Race condition параллельного refresh | ✅ закрыт атомарным claim'ом + покрыт юнит-тестом («parallel race») |

Быстрые проверки: secure-cookie ✅ (подтверждён код), **ws query-token убран** ✅ (ws.js — только `handshake.auth.token`, фронт уже слал auth.token), migrate.js/.env и Nginx WS-upgrade — при первом staging-деплое, CSP vs Stripe.js — на staging с реальными ключами.

Проверки этапа: сервер **149/149** (+14 rotation/lockout, +6 sanitize), фронт 58/58 ✅, E2E 44/44 ✅ (WS-тесты подтверждают отказ от query-token), lint 0 errors ✅. Живая проверка: login → refresh 200 → replay старого refresh 401 «reuse» → новый токен тоже отозван (семья) ✅. **XSS в «О себе»**: `<script>` больше не хранится — `stripHtml` на входе (profile PUT/register/посты/сообщения), см. AGENTS #32; полная верификация 22.08: check:ports ✅, build ✅, 251/251 ✅, смоук API 200 ✅.

### ✅ Этапы 35–37 (23.08.2026) — schema-validation в CI, слепые зоны тестов, IAP SDK

| Что | Чем закрыто |
|---|---|
| Schema validation в CI (kimi, инфра #1) | **Этап 35**: `scripts/schema-validate.mjs` — mysql_schema.sql ↔ information_schema живой БД (53 таблицы, exit 1 при расхождении); шаг в CI server-test после Init DB и на deploy ДО `migrate.js`; попутно убраны 4 мёртвые колонки схемы (users.email_verified_at, user_photos.moderated_by/at, messages.read_by) |
| Migration idempotency (слепая зона) | **Этап 35**: найден реальный баг — migrate.js падал на 018 (`Duplicate entry 'romantic'`), т.к. seed/ALTER-миграции 018–023 не идемпотентны; исправлено (INSERT IGNORE, уникальный ключ uq_icebreaker_q, ALTER через information_schema+PREPARE); двойной прогон → 0 errors |
| Security headers supertest (слепая зона) | **Этап 35**: `server/src/__tests__/security.headers.test.js` — 7 тестов против живого сервера (CSP, XFO/frame-ancestors, nosniff, HSTS, Referrer-Policy, /health без auth, кеш auth-эндпоинтов); skipIf сервер не поднят |
| Impersonation guard (слепая зона) | N/A — фичи имперсонации в коде нет |
| CI guard для Android cleartext (инфра #5) | **Этап 35**: `usesCleartextTraffic="true"` убран из main-манифеста (MITM-риск), добавлен debug-only манифест; guard `scripts/check-native-config.mjs` встроен в build:native и CI frontend job |
| Backup retention 7 дней мало (инфра #7) | **Этап 35**: 7 → 30 дней (backup-mysql.ps1 + .sh) |
| Down-миграций нет (инфра #8) | **Этап 35**: отмечено в docs/rollback-plan.md («откат только через бэкап») |
| RevenueCat SDK (kimi #3) | **Этап 36**: `@revenuecat/purchases-capacitor@13.4.1`, `require()` → динамический `import()` (ESM-бандл); graceful no-op сохранён; остался только VITE_REVENUECAT_API_KEY перед APK |
| AGENTS.md adminAuth (qwen #1) | **Этап 37**: ПРАВИЛО adminAuth в Security Rules — только active-check middleware, запрет пассивных `next()`, ручных проверок роли и обхода единого гейта |

Проверки этапов: сервер **156/156** (+7 headers), фронт 58/58 ✅, E2E 44/44 ✅ = **258/258**; lint 0 errors ✅; build ✅; migrate.js ×2 → exit 0 ✅; schema-validate 53 таблицы OK ✅; cleartext guard OK ✅.

### Остаток безопасности после этапа 35 (без TOTP/CSRF/fingerprint)

| # | Пункт | Кто флагил |
|---|---|---|
| 4 | **Admin TOTP 2FA** для /api/admin/* — сейчас один фактор (admin@mail.ru/demo123456) | kimi 🔴 |
| 5 | **CSRF double-submit token** — SameSite=Lax не защищает от same-site атак (поддомены, open redirect); критично при выносе API на отдельный домен | все три 🔴/🟠 |
| 6 | Привязка сессии к устройству/IP (fingerprinting) | kimi 🟡 |

### Инфраструктурные находки аудита (этапы 35+)

1. ~~**Schema validation в CI**~~ ✅ этап 35 (см. таблицу выше)
2. **EXPLAIN/SQL-аудит статически в CI** перед каждым PR (qwen)
3. **Redis fallback под нагрузкой**: REDIS_URL=invalid → кэш-промахи не должны DDOS'ить MySQL, queue fallback не теряет письма (kimi)
4. **Docker Compose up реально не прогонялся** — поднять на staging, проверить healthcheck/volumes/сеть (все три)
5. ~~**CI guard для Android**~~ ✅ этап 35: cleartext убран из release, guard в build:native и CI
6. Circuit breaker для Stripe/OpenAI/S3 — не копить очередь webhook'ов при недоступности (после релиза) (kimi)
7. ~~Backup retention 7 дней мало~~ ✅ этап 35: 30 дней (платежи ~1 год — после Stripe live) (kimi)
8. ~~Down-миграций нет в migrate.js~~ ✅ этап 35: отмечено в rollback-plan.md
9. Внешний мониторинг: UptimeRobot + Grafana alerts в Telegram/Slack (CPU, 5xx, DB connections) — «если VPS умрёт, никто не узнает» (qwen, дипсик)
10. Log aggregation (Loki/ELK) вместо Winston-на-диск + SSL Let's Encrypt auto-renewal (после релиза) (kimi)
11. Windows-центричность: при переезде на Linux VPS адаптировать .bat→.sh (backup, запуск) (qwen, дипсик)
12. Разбить AGENTS.md — сейчас ~1180 строк (qwen называл >1500 — завышено) (qwen, дипсик)
13. ~~��������������� ������� ���������� ������� ������ (.env)~~ �������: ���� 41 docs/runbook-keys.md (������� ������� ������� ����� + ������� ��� ������)

### Слепые зоны тестов (бэклог этапа 36+, консолидировано из kimi + дипсик)

- Redis fallback integration (ECONNREFUSED → приложение живёт)
- Refresh race E2E: два окна одного юзера, быстрый refresh в обоих
- Cookie edge cases: expired cookie mid-session, revoked refresh, concurrent tabs
- ~~File upload security: 15MB отклоняется, `.php.jpg`, path traversal `../../etc/passwd` → сохраняется как uuid~~ �������: ���� 39 (+5 ������ � upload.test.js)
- ~~Rate-limit spec: 61-� ������ � ������ -> 429~~ �������: ���� 40 (ratelimit.test.js, 3 �����)
- WS reconnect + exponential backoff (эмуляция обрыва мобильного интернета)
- GDPR full erasure cascade E2E (юзер → активность → erase → 404 везде)
- ~~Stripe webhook security unit: invalid signature → 400, replay, idempotency-key без дублей~~ �������: ���� 39 (premium-webhook.test.js, 5 ������; ��������������� ����� webhook_events)
- RevenueCat webhook E2E с моком событий (дипсик)
- ~~i18n completeness unit~~ �������: ���� 40 (i18n.test.ts; ������� ������� zodiac x12 en + circadian x5 ru)
- Backup/restore smoke в CI (restore → smoke-тесты)
- ~~Migration idempotency: migrate.js дважды подряд → 0 errors~~ ✅ этап 35 (и найден реальный баг 018)
- ~~Security headers supertest (CSP/HSTS/XFO на /health)~~ ✅ этап 35 (7 тестов, skipIf без сервера)
- ~~Impersonation guard: админ→админ → 403~~ N/A — фичи имперсонации нет
- Cross-browser: projects [chromium, firefox, webkit] в playwright.config.ts
- IAP sandbox-тест покупок после публикации в Google Play (дипсик)

### Приоритизация (фазы qwen, консенсус)

1. **«Первый рубль» (1–2 нед)**: staging VPS + GitHub secrets → ключи (Stripe test→live, SMTP, Sentry DSN) → E2E на staging с реальными ключами → docker compose up
2. **«Мобильный релиз» (2–3 нед)**: домен + SSL → keystore + AAB → App Links (assetlinks.json + SHA256) → RevenueCat проект + ключ → Google Play (17+)
3. **«Надёжность» (параллельно)**: k6 100 VU на staging → UptimeRobot + алерты → тест плана отката → этап 34 (безопасность) → чистка AGENTS.md
4. **«Масштабирование» (после релиза)**: S3/CDN → FCM → Twilio → Rekognition/OpenAI фото-модерация → Storybook

## 📌 Правила для будущих этапов (из этапа 18)

- **Express: middleware порядка** — Sentry errorHandler обязан стоять ДО финального 500-хендлера, requestHandler — до роутов
- **Миграции** — только через `database/migrations/` + idempotent (information_schema + PREPARE, MySQL 8.4)
- **`migrate.js` не запускается из `server/`** — модуль в `database/migrations/`, mysql2 резолвится оттуда; применяли SQL напрямую + `INSERT INTO _migrations`
- **Пустые catch** — обязателен комментарий (линт no-empty)
- **`any`** — warn (решение этапа 17), новые ошибки lint не вносить
- **CI-миграции (этап 27)** — в deploy.yml миграции выполняются НА СЕРВЕРЕ перед pm2 restart (`set -a; . ./server/.env; node database/migrations/migrate.js`), не с CI-раннера

## ����� 38-41 (23.08.2026)

| ���� | ��� ������� |
|---|---|
| **���� 38** | Admin TOTP 2FA: �������� 026 (users.totp_secret/totp_enabled), server/src/totp.js (otplib v13), routes/totp-2fa.js (setup/enable/disable ����� adminAuth), login ������� totp_code ��� ���������� 2FA (401 TOTP_REQUIRED/TOTP_INVALID + lockout), UI two-fa-settings.tsx, i18n two_fa_*; ����� ����� 8/8 |
| **���� 39** | Webhook replay/idempotency: �������� 027 webhook_events (UNIQUE provider+event_id), INSERT IGNORE ����� � checkout.session.completed, premium-webhook.test.js 5/5; upload security +5 ������ (non-image/no-file/auth/traversal/double-ext) |
| **���� 40** | �������� �������� � middleware/limiters.js (������� ��� ������); ratelimit.test.js 3/3 (60 �� -> 61-� 429, api 600, �������� �����������); i18n.test.ts RU<->EN ������������; E2E icebreakers ������� (������ ������ - �������� ���������� �������� �� 199) |
| **���� 41** | docs/runbook-keys.md: ������� ���� ������ (.env vs VITE_*), ������� ��� ������� ����������, ������� ������ ������� ��� ������ |

�������� ����� ����� 41: server 179/179 (18 ������), front 61/61 (13 ������), E2E 44/44 = **284/284**

## ������ ���������� ������� kimi/qwen/������ �� 23.08.2026 (�������� ����� ����� 37, �������� � ��� 258 ��������)

### ������� ������� 38-42
| ������� ������ | ���� |
|---|---|
| Admin TOTP 2FA ����������� (kimi 1.5, ������ #1, qwen ����-3) | 38: TOTP + lockout + UI + ����� 8/8 |
| File upload security ������ ��� (#4) | 39: +5 ������ |
| Stripe webhook replay/idempotency (#8) | 39: webhook_events ����� + 5 ������ |
| Rate-limit spec 61->429 (#5) | 40: ratelimit.test.js 3 ����� |
| i18n completeness (#11/#10) | 40: i18n.test.ts + �������� ������� zodiac/circadian |
| Redis fallback: ECONNREFUSED ����� ������� ���������� (kimi 2.3/5.1 - ���� ���������� ����� ��������!) | 42: probe � queue.js -> ������� ����������� graceful; process-������ |
| __Host- ������� cookie (kimi 1.2) | 42 |
| Cleanup revoked refresh-������� cron (kimi 1.3) | 42: cleanup.js, ��� � �����, 30 ���� |
| RevenueCat webhook ��� ����� (������ #9) | 42: iap-webhook.test.js 6 ������ |
| WS reconnect race ����� silent refresh (kimi 1.4) | ��������������: use-websocket.ts ���������� ����� �� [token] + backoff 1s->30s |
| CORS ��� ������� vs CORS_ORIGIN (kimi 1.6) | ��������������: prod fail-fast ������� CORS_ORIGIN; ������� ����� Bearer, CORS �� ��������� |
| deploy.yml cd ����� migrate.js (qwen) | ��������������: cd /app/swiftmatch ���� |
| Security headers regression (#10 kimi) | 35: security.headers.test.js ��� � CI |

### �������� (������� ������� - �� ���)
- Staging VPS + docker compose up end-to-end + healthcheck/volumes/���� (kimi 2.1, ��� ������ ���� 0)
- �������� �����: Stripe live/test, SMTP, Sentry DSN, S3, Twilio, OpenAI, FCM, REVENUECAT_WEBHOOK_SECRET, VITE_REVENUECAT_API_KEY
- ����� + SSL Let's Encrypt auto-renewal (certbot) + App Links assetlinks.json + SHA256 + keystore/AAB + Google Play 17+
- k6 100 VU �� staging � ������������ free -m / Threads_connected (kimi 2.2); �������� DB_POOL_MAX 20->10
- UptimeRobot + Grafana alerts � Telegram/Slack; log aggregation Loki/ELK (����-�����)

### �������� (���, ������ ��������� / ����� ������)
- CSRF double-submit - ������ ��� ������ API �� �������� (������� ������������� �� ���� ������x)
- Fingerprint �������� refresh-������ � ����������/IP (������ #3) - ���� ������� ��������� Bearer-flow, ������ ��������� ����� ����
- Circuit breaker Stripe/OpenAI/S3 (������ #4, kimi) - ����� ������
- E2E: refresh race (2 ����), cookie edge cases, GDPR erasure cascade, cross-browser firefox/webkit, TOTP enrollment E2E � QR
- Backup/restore smoke � CI; RTO-����� � rollback-plan.md (kimi 2.5): �������� scripts/verify-backup.ps1 � �������� ������ restore
- Rate-limit/lockout �� Redis store ��� �������������� ��������������� (kimi 1.1) ��� ����������������� single-instance � rollback/deploy
- Nginx proxy_read_timeout 86400s ������ ��� location /socket.io/ (kimi 2.6) - ��������� ������ ��� ������
- ������� AGENTS.md (>1500 ����� �����); Node 25 dev vs 22-alpine Docker - ������� ��������� jsdom/localStorage

�������� ����� ����� 42: server 191/191 (21 ����), front 61/61 (13 ������), E2E 44/44 = **296/296**