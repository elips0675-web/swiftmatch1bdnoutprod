# SwiftMatch1BD — снимок для AI-анализа (август 2026)

> Самодостаточный контекст для аудита моделями (kimi / qwen / deepseek и т.д.).
> Свежие детали: «Что сделано.txt» (этап 35 = Hangouts), «Что доделать.txt» (журнал до этапа 46),
> AGENTS.md (грабли #1–52), context.txt, project-context.md, README.md.
> Предыдущие оценки: «Оценка kimi.txt», «Оценка qwen.txt», «Оценка дипсик.txt».

## Что это за проект

Датинг-платформа (Tinder-like) + соцфункции: React 18 + TS + Vite 8 фронт,
Node.js/Express + MySQL (mysql2) бэкенд, Socket.IO real-time, Capacitor Android.
Локальная тестовая копия с реальной БД (50 демо-юзеров). Порты: фронт 8081
(vite proxy /api→3002), API+WS 3002, MySQL 3306, Redis 6379.

## Статус проверок (всё зелёное)

- Тесты **369/369**: сервер 246/246 (25 файлов), фронт 68/68 (15 файлов), E2E 55/55 (9 spec: audit-full, features, hangouts, partners, security-blind-spots, login, profile, register + cross-browser по флагу)
- vite build ✅, eslint 0 errors (154 warnings — стилевые), Swagger ✅
- E2E: Playwright, storageState из global-setup (.auth/*.json), аудит console-errors на каждой странице, screenshot/video/trace на падение
- Pre-flight: `npm run check:ports` (порты + JWT_SECRET), `scripts/check-keys.ps1` (какие прод-ключи не заполнены)

## Безопасность (текущая модель)

- **JWT в httpOnly cookie** `sm_token` (24h) + `sm_refresh` (7d): SameSite=Lax, Secure в production.
  Ставятся на login/register/refresh/dev-login (`server/src/cookies.js`).
- Все auth-мидлвари читают **Bearer ?? cookie** (`extractToken`) — нативные сборки работают через Bearer.
- Web-фронт НЕ пишет токен в JS-доступный storage (XSS-safe), читает легаси-storage для совместимости;
  refresh через POST с пустым body (сервер берёт куку), защита от цикла (flag `refreshed`);
  `GET /api/auth/me` — probe, всегда 200 `{authenticated:false}` без сессии;
  `POST /api/auth/logout` — чистит куку + удаляет refresh-токен из БД.
- **Refresh rotation + reuse detection (этап 34)**: каждый refresh-токен принадлежит `family_id`;
  ротация атомарно отзывает старый токен и выдаёт новый в той же семье; replay ротированного =
  компрометация → отзыв всей семьи. Гонка параллельных refresh закрыта atomic claim'ом.
- **Revoke all sessions**: POST `/api/auth/logout-all`; смена пароля (reset-password) отзывает все сессии юзера.
- **Account lockout**: 5 неудачных логинов подряд на email → 429 на 15 мин (`server/src/lockout.js`, env `AUTH_LOCKOUT_MAX_ATTEMPTS`).
- **Санитизация ввода**: пользовательский текст хранится без HTML-тегов (`stripHtml` в profile PUT, register, постах групп, сообщениях; «<3» и «a<b» не ломаются). В коде нет dangerouslySetInnerHTML — React экранирует рендер.
- WS handshake принимает токен только из `handshake.auth.token` (query-param убран).
- CSRF: SameSite=Lax + строгий CORS_ORIGIN (fail-fast в production без него).
  Полные CSRF-токены отложены до выноса API на отдельный домен (осознанное решение, см. AGENTS Security Rules);
  Admin TOTP 2FA и fingerprinting сессий — этап 35+ после staging.
- Helmet (CSP и др.), request-id, Sentry c PII-фильтром в beforeSend, Winston без тела запросов.
- Rate-limit: глобальный 600/мин, лайки 30/мин, auth 60/мин (поднят для E2E) + лок-аут логина.
- Модерация: banned-words в чатах, auto-escalation репортов (3+ → temp ban, severe → мгновенный).

## Инфраструктура

- CI/CD GitHub Actions: lint → front tests → server tests (MySQL-сервис) → E2E → deploy (SCP + pm2);
  миграции выполняются НА СЕРВЕРЕ перед pm2 restart (атомарно, секреты DB_* не нужны).
- Redis: кэш (profile 60s, matches 30s), Bull Queue (email/push/image), Socket.IO adapter; graceful fallback.
- Мониторинг: Prometheus :9090 + Grafana :3001 (7 панелей), prom-client метрики HTTP/DB/WS/cache.
- Docker Compose: app + nginx + Prometheus + Grafana (YAML валиден, локально не поднимался — нет Docker на машине).
- Nginx conf: rate-limit, WS 86400s, SSL, SPA fallback. Бэкапы MySQL: скрипты PS/sh + Task Scheduler 03:00, retention 7d.
- k6: лайт-прогон 10 VU ок (p95 ≤ 232ms, 57% «ошибок» = rate-limit by design); полный 100 VU — на staging.

## Последние этапы (30–33)

- **30:** починен 500 на `/api/invites` (дрейф колонок код↔таблица); смоук 18 API-точек.
- **31:** EXPLAIN-аудит ВСЕХ SQL против схемы: найдены скрытые 500 — iap.js webhook ссылался на
  несуществующую `subscriptions.updated_at`, location.js использовал `user_profiles.user_id` вместо `id`. Починены.
- **32:** Android toolchain установлен (SDK 35/36, JDK 21 — Gradle 8.14.3 не работает на Java 25, AGP 8.13.2);
  `@revenuecat/purchases-capacitor@13.4.0`; **debug APK собран** (27,8 MB, `android/app/build/outputs/apk/debug/app-debug.apk`).
  Грабля: `compressDebugAssets` MD5-jar лечится standalone-прогоном задачи.
- **33:** миграция на httpOnly cookie (см. «Безопасность») по ADR; все тесты обновлены и зелёные.
- **34:** refresh rotation + reuse detection (отзыв семьи), logout-all, revoke при смене пароля, account lockout 5→15 мин, ws.js без query-token. Сервер 143/143 (+14 тестов).
- **35–45 (кратко):** schema-validate в CI + идемпотентные миграции; Admin TOTP 2FA (otplib); Stripe webhook idempotency (webhook_events); rate-limit spec-тесты; i18n completeness; Redis probe fallback (нет Redis → очереди graceful); __Host-cookie в prod; cleanup revoked refresh; XSS sanitize.js усилен (on*, javascript:, alert-зонды) + миграция 029 чистки легаси; E2E security-blind-spots (GDPR cascade, refresh race, cookie edges).

## Этап 46 — фича «Встречи» (Hangouts) + живые уведомления (23.08.2026)

- Миграция 028: `hangouts` / `hangout_responses` / `hangout_chats`, флаг `hangouts_enabled`.
- API `hangouts.js`: лента с гео-радиусом и категорией, CRUD (soft-cancel), respond/accept/decline/cancel;
  при accept — авто-чат + hangout_chats + уведомления обеим сторонам; max_companions → статус completed.
  Админ-модерация block/unblock (`admin/hangouts.js`). Категории: cinema/theater/exhibition/cafe/concert/sport/other.
- Страницы: /hangouts (лента+фильтры), /hangouts/create, /hangouts/my (табы), /hangouts/:id; админка /admin/hangouts;
  баннер «Куда пойдем» на Home с гейтингом. WS: new_response/response_accepted/cancelled → рефетч на my/detail.
- Живые уведомления: `notifications.js` (GET c JSON_EXTRACT-обогащением actor_name/hangout_title, read-all, :id/read),
  колокольчик app-header.tsx на реальных данных (демо — fallback гостей), бейдж+тост по WS notification:new,
  клик = deep-link по типу уведомления, «Прочитать все». Типы: hangout_response/accepted/declined/cancelled, invite, like.
- Контекст встречи в чате: GET /api/hangouts/by-chat/:chatId (403 не-участнику) + баннер над сообщениями чата.
  Попутно починен pre-existing баг: отсутствовал GET /api/chats/:chatId — страница чата падала HTML-404.
- Тесты этапа: сервер hangouts.test.js 33 + notifications.test.js 7 + XSS-чтение профиля; фронт hangouts.test.tsx 5 +
  home-hangouts.test.tsx 2; E2E hangouts.spec.ts 4 (создание→отклик→accept→чат).
- Хвосты (не блокируют): UI редактирования объявления (PUT есть), date_from/date_to фильтры, пагинация, лимит 1/день для free.

## Следующая большая задача — партнёрская экосистема (Mesta.txt)

Единый движок партнёров (10 типов: афиша, рестораны, цветы, такси, отели, спа, фотографы, психологи, стриминг, эвенты):
миграция 030 (partners/partner_offers/partner_conversions), роутер partners.js (offers c гео/placement + track
c utm_source=swiftmatch&ref), chat-partner-actions.tsx («Такси/Ресторан/Кино» в чате), выбор «из афиши» в hangout-create,
admin-partners.tsx, флаг partnerOffersEnabled. Wave 1 = deeplink-партнёры без сложных API. План и DoD — в part.txt («Доделать»).

## Этап 47 — партнёрская экосистема Wave 1 (23.08.2026)

- Миграция 030: `partners` (api/deeplink/saas, commission_rate, UNIQUE name), `partner_offers`
  (10 категорий, deeplink, placement SET hangout/chat/profile/passport/attachment_result, valid_from/to),
  `partner_conversions` (click/booking/purchase/lead + commission), флаг `partner_offers_enabled`.
  Сид: Yandex Go (yandextaxi://), KinoPoisk Afisha, Restoclub, Ostrovok.
- API: GET /api/partners/offers?category=&city=&placement=&lat=&lng=&radius= — FIND_IN_SET по SET-placement,
  гео через ST_Distance_Sphere + HAVING distance_m < ?, только active+valid даты; POST /api/partners/track
  → INSERT conversion + deeplink с utm_source=swiftmatch&ref={referral_code}.
- Админ-CRUD admin/partners.js: относительные пути (грабля #53 — при монтировании на /api/admin путь
  '/api/admin/x' даёт двойной префикс и тихий 404). Страница /admin/partners: партнёры со статистикой
  clicks/conversions/commission_pending, офферы, pause/activate, диалоги создания.
- Фронт: chat-partner-actions.tsx (чипы над вводом чата, http → window.open, схема-ссылки → fallback-диалог),
  «Выбрать из афиши» в hangout-create (placement=hangout, автозаполнение формы + lead-конверсия),
  блок «Где остановиться» в settings-privacy при passport_mode+городе (placement=passport).
- Тесты этапа: сервер partners.test.js 13, фронт chat-partner-actions.test.tsx 3, E2E partners.spec.ts 4.
  Итог 369/369. Хвосты: подстановка плейсхолдеров {lat}/{to_lat}/{city} в deeplink'ах, S2S postback для booking/purchase,
  Wave 2–4 из Mesta.txt.

## Реализованный функционал (кратко)

Знакомства: анкета, геопоиск (MySQL Spatial), Smart Matching, attachment-тест, лайки/мэтчи/чаты,
группы, конкурсы, блокировки, AI Icebreakers (OpenAI или fallback из БД), реакции, TTL-сообщения,
Video Date Scheduling, Safety Check-in, Profile Score, Ghost Mode + Passport Mode (premium gate),
**Встречи/Hangouts** (объявления «Куда пойдем», отклики, авто-чат при accept, модерация),
живые уведомления (колокольчик + WS).
Монетизация: Stripe Checkout (idempotency-key), RevenueCat IAP-клиент, реклама через фича-флаги, тарифы Plus/Gold/Platinum.
Админка: дашборд, аналитика (retention/revenue-mix/регистрации), юзеры (бан/имперсонация), фича-флаги,
модерация, контент, email-кампании, A/B-эксперименты (стабильный assign по MD5), модерация hangouts.
GDPR: consent при регистрации (consent_log), data export, erasure requests — покрыто E2E.
i18n RU/EN, push VAPID + FCM hook, deep links hook, рефералка.

## Честный список «не сделано»

1. Staging-сервер: нужен VPS + secrets DEPLOY_HOST/USER/SSH_KEY; полный k6 и Docker-up только там.
2. Реальные ключи: Stripe LIVE, SMTP, Sentry DSN, S3, Twilio, OpenAI/Rekognition, FCM_SERVER_KEY,
   REVENUECAT_WEBHOOK_SECRET (+VITE_REVENUECAT_API_KEY сейчас плейсхолдер). Без ключей всё деградирует мягко
   (mock/fallback), кроме Stripe-webhook и RevenueCat-webhook (осознанные 502/503 в prod).
3. Релиз Android: домен вместо LAN-IP, keystore-подпись, App Links (assetlinks.json), AAB + Google Play (политика 17+, сторы).
4. Полная CSRF-защита (double-submit token) — только при выносе API на отдельный домен.
5. Design System/Storybook — не начато.
6. Хвосты Hangouts: UI редактирования объявления, фильтры дат, пагинация ленты, лимит 1 объявление/день для free.
7. Партнёрская экосистема (Mesta.txt) — не начата; план в part.txt («Доделать»).

## Ключевые файлы

| Файл | Назначение |
|---|---|
| `server/src/index.js` | Express-корень: helmet/cors/rate-limit/versioning/admin-gate |
| `server/src/cookies.js` | setAuthCookies/clearAuthCookies/extractToken (Bearer ?? cookie) |
| `server/src/middleware.js` | auth/optionalAuth/errorHandler |
| `server/src/routes/*.js` | auth, profile, social, chats, hangouts, notifications, premium, stripe, iap, gdpr, location, invites… |
| `server/src/ws.js` | Socket.IO: чаты, typing, online, TTL-cleanup, notification:new, hangout:* |
| `src/lib/token.ts` | память на web (запись), чтение легаси-storage, sessionStorage на нативе |
| `src/lib/api.ts` | fetch-обёртка: Bearer, один авто-refresh, auth:unauthorized event |
| `src/context/auth-context.tsx` | restore через /me → dev-login fallback, logout через API |
| `src/lib/native.ts` | нативный адаптер fetch/WS (Bearer вместо cookie) |
| `e2e/setup/global-setup.ts` | health-check API + сеет storageState (localStorage.token!) |
| `database/migrations/` | 29 нумерованных миграций + migrate.js |

## Демо-доступы

admin@mail.ru / user2@mail.ru / user4..user50@mail.ru — пароль `demo123456`; user1@mail.ru забанен.
dev-login (только не-production): POST /api/auth/dev-login {email} → токен + куки.

## Что просим у модели

Свежий аудит: пробелы безопасности (особенно новая cookie-схема), риски миграции на прод,
приоритизация оставшегося списка, слепые зоны тестов.
