# SwiftMatch — Production Context

## Stack
- **Frontend:** React 18 + Vite 8 + TypeScript 5 strict + Tailwind CSS 3 + shadcn/ui
- **Backend:** Express.js 4 + MySQL (mysql2) + Socket.IO 4
- **State:** TanStack Query 5 + React Context
- **Forms:** React Hook Form + Zod 4
- **Auth:** JWT (Bearer) + refresh tokens, dev-login для админки
- **Payments:** Stripe (mock-fallback без ключа) + RevenueCat IAP scaffold
- **Realtime:** Socket.IO (chats, online status, typing indicator)
- **Monitoring:** Sentry (установлен, ждёт DSN)
- **Logging:** Winston (JSON-формат)
- **i18n:** Custom LanguageContext (RU/EN)
- **Push:** VAPID Web Push + FCM (dual-канал)
- **Mobile:** Capacitor (Android APK), AdMob v6.0.0, Deep Links scaffold

## Ports
- Frontend: **8081** (Vite dev server, proxy → localhost:3002)
- API: **3002** (Express)
- DB: 3306 (MySQL via Laragon)

## Auth
- `adminAuth` middleware — **ACTIVE** (с этапа 9: 401 без токена, 403 не-админ; публичный только `GET /api/admin/features`)
- AdminGuard при отсутствии Supabase делает dev-login
- CORS — `*` (для Capacitor)

---
## Последние изменения (этапы 13–24, август 2026)
- **13** — `запуск-всего.bat` исправлен (пути C:\swiftmatch1bdnoutprod, порт API 3002, подъём MySQL); `.env.example` проверен байтами — UTF-8, в порядке (кракозябры = консоль CP866)
- **14** — production-smoke зелёный (`NODE_ENV=production`): /health, auth, админка (401 анониму), features public, icebreakers → fallback БД
- **15** — `scripts/check-keys.ps1` переписан в ASCII (PS 5.1 не читал UTF-8 без BOM, падал на «—»); REDIS_URL в .env реально задан — очереди работают
- **16** — README: «🚀 Чек-лист запуска продакшена» (ключи → миграции → проверки → запуск из server/ → поведение без ключей → CI-secrets)
- **17** — lint: 0 errors / 150 warnings (no-explicit-any → warn, 18 catch помечены, require Capacitor — line-disable, пустой interface → type); 🐛 исправлен `src/hooks/useApi.ts` — useMemo ВНУТРИ useEffect (нарушение rules-of-hooks) + двойной fetchData() (дубли запросов при каждом маунте); playwright-report/, test-results.json → .gitignore
- **18** — qwen-аудиты: Sentry initSentry/registerSentryErrorHandler (порядок middleware), dev-login 404 в production, CORS fail-fast без CORS_ORIGIN, транзакции подписок (mock+webhook), миграция 024 (drop is_liked/is_bookmarked, INDEX idx_chats_group)
- **19–20** — юридические страницы существовали (ссылки в регистрации/настройках) → добавлена ссылка на ToS в чекбокс; age-gate 18+ (обязательный чекбокс + тост)
- **21** — `scripts/verify-backup.ps1` (restore в скретч-БД + sanity: users/matches/messages/subscriptions/feature_flags, PASS 130 users/213 messages); `install-backup-task.bat` — daily backup + weekly verify (Пн 03:30)
- **22** — RevenueCat IAP-клиент: `src/lib/iap.ts` (guarded require, offerings/purchase, productId маппинг), premium.tsx нативная ветка с фолбэком; требуется `npm i @revenuecat/purchases-capacitor` + `VITE_REVENUECAT_API_KEY` при сборке APK
- **23** — k6: починен дрейф контрактов (`/api/users/search`, `liked_user_id`, динамический chatId, креды user5@mail.ru); лайт-прогон 10 VU: p95 < 250 ms, 5–8 ms/запрос; полный ramp 100 VU — только staging (rate-limit'ы 600/мин глобальный + 30/мин лайки)
- **24** — product analytics: воронка расширена — login_completed, register_completed, message_sent (+ttl), purchase_completed (+tier/duration/channel); retention/revenue-mix/registrations уже в админке; PostHog не нужен (self-hosted)
- **25–26** — UI: кнопка «Открыть профиль» в поиске (variant_b) уменьшена до пилюли `h-9 px-8 rounded-full` и отцентрирована по высоте между рядом лайк-кнопок и BottomNav (`mt-8 mb-8` в зазоре `pb-24`)
- Тесты после этапов 22–26: E2E 44/44 ✅, фронт 57/57 ✅, сервер 126/126 ✅, tsc ✅, build ✅, lint 0 errors

---
## Что сделано

### Платежи + Коммуникации
- **Stripe:** Idempotency-Key middleware, STRIPE_LIVE флаг
- **SMTP:** Nodemailer + retry (3 попытки, exponential backoff), graceful skip без ключа
- **Sentry:** `@sentry/react` + `@sentry/node`, beforeSend с PII scrubbing, requestHandler/errorHandler

### Инфраструктура
- **Docker:** `.dockerignore`, healthcheck (curl /health), `mem_limit: 512m`, `JWT_SECRET` через env
- **Nginx:** rate limit (api 30r/s, auth 5r/s), WebSocket 86400s, SSL, SPA fallback
- **CI/CD:** `.github/workflows/deploy.yml` — lint → frontend test → server test (MySQL сервис) → E2E → deploy; в lint-джобе `npm run check:ports`
- **Миграции БД:** `database/migrations/` — нумерованные .sql + migrate.js
- **MySQL backup:** `scripts/backup-mysql.ps1` + `.sh`, retention 7 дней
- **Setup:** `scripts/setup.ps1` — авто-копирование .env + генерация JWT_SECRET + npm install
- **S3 scaffold:** lazy-init (AWS env → multer-s3, иначе локальный диск)
- **Redis scaffold:** lazy singleton с ioredis, getRedis()/withRedis()/disconnectRedis(), winston-логи
- **Redis scaffold + cache module:** lazy singleton с ioredis, getRedis()/withRedis()/disconnectRedis(), winston-логи. `cache.js` — `cacheRoute()`, `cacheRoutePerUser()`, `setCached()`/`getCached()`, `invalidate()`. Кэширует GET /api/profile/:id (60s) и GET /api/matches (30s per-user). **Redis Adapter для Socket.IO** — уже в `ws.js` (читать `getRedisPub`/`getRedisSub`, крепит adapter при наличии Redis)
- **CACHE_TTL:** env-переменная (`server/.env.example`), по умолчанию 60s
- **Disappearing Messages (TTL):** миграция `011_add_disappearing_messages.sql`, колонка `ttl_seconds` на `messages`, `startMessageCleanup()` в `ws.js` (интервал 10s, удаление истёкших + WS `chat:message-deleted→user:{id}`), POST /api/chats/:chatId/messages принимает `ttl_seconds`, TTL selector Popover в чатах (Off/5s/30s/1m/5m/1h/24h)
- **Video Date Scheduling:** миграция `012_add_date_schedules.sql`, роуты `/api/schedule` (GET/POST/PUT), WS `schedule:updated`, `src/pages/schedule.tsx` (список + фильтр + accept/decline/cancel), Calendar button в заголовках чатов, диалог предложения даты с выбором времени/длительности/сообщения
- **Profile Score:** миграция `014_add_profile_score.sql`, колонка `profile_score` в `user_profiles`, `calculateProfileScore()` (полнота анкеты: аватар, био, рост, образование, знак зодиака, стиль привязанности, цель, город, пол, кого ищет, 3+ фото), `GET /api/profile/:id/score` возвращает score + рекомендации, UI-бейдж в профиле (зелёный ≥80, жёлтый ≥50, красный <50) + список рекомендаций с потенциальными баллами
- **Background GPS + Geofence:** миграция `011_add_background_gps.sql`, `PUT/GET /api/location`, `use-background-geolocation.ts` (Capacitor geolocation + fallback watchPosition, 5min polling), geofence-события
- **CI/CD:** `.github/workflows/deploy.yml` — lint → frontend test (55) → server test (MySQL контейнер, 124) → E2E (Playwright, 30) → deploy (SCP + pm2, только main)
- **Docker Compose:** `docker-compose.yml` — app (node:22-alpine, healthcheck `/health`) + nginx + Prometheus (`:9090`) + Grafana (`:3001`), named volumes
- **Monitoring:** `server/src/metrics.js` (prom-client: http_requests_total/duration, db_queries_total/duration, ws_events_total, cache_hits/misses), `monitoring/prometheus.yml` (scrape `/metrics` every 15s), `monitoring/grafana-dashboard.json` (7 panels: HTTP rps, p50/p95/p99, DB queries, WS events, cache, memory, CPU)
- **Load Testing:** `k6/load-test.js` — ramp-up 10→100 users over 60s, checks 6 endpoints (health, login, search, premium, matches, chats)

### Аудит по оценке kimi (июль 2026) — 13 требований
- **Admin роуты:** проверены все 8 файлов — 30 тестов проходят
- **Stripe:** express.raw ДО express.json, Idempotency-Key, mock заблокирован в production
- **Sentry beforeSend:** PII scrubbing — authorization, cookie, email, IP
- **Docker:** .dockerignore, restart, mem_limit, named volume, healthcheck
- **Push Notifications:** ✅ VAPID Web Push + FCM dual-канал (`fcm.js`, `push.js`); ждёт FCM_SERVER_KEY
- **SMS Verification:** ✅ Интегрирован в регистрацию (auth.js + register.tsx); ждёт Twilio API-ключи
- **AI Moderation:** ✅ Интегрирован в upload.js (NSWF → moderation_status='flagged'); ждёт API-ключи
- **Image Pipeline:** ✅ Sharp resize 800/400/200 + WebP/AVIF; CDN/S3 — ждёт настройку
- **WebRTC:** ✅ RTCPeerConnection + STUN + сигналинг; **Video Date Scheduling реализован** (schedule.js + schedule.tsx + WS schedule:updated, E2E этап 10)
- **RevenueCat/IAP:** ✅ Scaffold (webhook + products + status, миграция 008); ждёт RevenueCat проекта
- **Deep Links:** ✅ Scaffold (assetlinks.json, capacitor.config.ts, use-deep-links.ts); ждёт домена
- **Nginx:** rate limit, client_max_body_size, proxy_read_timeout 86400s, SSL
- **Логи:** 0 console.log/error в server/src/ — везде Winston
- **Тесты:** 209/209, 0 failures — 55 фронт + 124 сервер + 30 E2E

### Функциональный аудит (июль 2026)
Проверены все 8 разделов через работающий API (сервер localhost:3002):
| Раздел | Статус | Найдено |
|---|---|---|
| **Авторизация** | ✅ | dev-login 200, login demo 200, refresh token работает, forgot/reset password — логика верна (SMTP ждёт ключей) |
| **Регистрация** | ⚠️ Починено | Не указывался `age` (колонка NOT NULL) → 500. Добавлен `age=18` в INSERT |
| **Профиль** | ✅ | GET /api/profile/2 — 200, данные + фото + интересы |
| **Лайки/Мэтчи** | ✅ | POST like (с auth) — проверена логика, daily limit, match detection. Routes: `/api/likes`, `/api/matches` |
| **Чаты** | ✅ | GET /api/chats — 200, 5 чатов, с read receipts. WS typing indicator + real-time |
| **Премиум** | ✅ | `/api/premium/tiers` 200, `/api/premium/my` 200, checkout mock 201, webhook — express.raw |
| **Группы** | ⚠️ | `/api/groups/:groupId` — есть и работает. Нет списка `/api/groups` (фронт не вызывает) |
| **Админка** | ✅ | stats 200, users 200 (22 total), features 200, campaigns 200, photos/pending 200 |
| **Модерация/Репорты** | ⚠️ Починено | Reports 500 — `r.evidence` колонка отсутствовала в БД, заменена на `NULL as evidence` |
| **Инфраструктура** | ✅ | /health 200, WS pingInterval 10s, Sentry beforeSend, helmet, CORS env |

### Bugfixes
- **double initSentry():** удалён дублирующий вызов без `app`
- **register 500:** INSERT в user_profiles не указывал `age` (NOT NULL) — добавлен `age=18`
- **reports 500:** SELECT использовал `r.evidence`, колонки нет в БД — заменено на `NULL as evidence`
- **premium-success.tsx crash:** `useSearchParams()` возвращает URLSearchParams, а не кортеж. `const [params]` → `const params`
- **Activity 404/500:** Route `/api/activity` полностью отсутствовал — создан в `social.js`
- **Activity SQL:** колонка `target_user_id` не существует (правильно: `target_id`), JOIN был на неправильное поле
- **Rate-limiter auth:** поднят с 10→30 req/min для `/api/auth/` (слишком жёсткий для E2E тестов)
- **chats.tsx Vite SyntaxError:** не хватало `</div>` для `<div data-testid="message-list">`, из-за чего Vite выдавал `Expected '</', got 'jsx text'` при компиляции chats.tsx. Ломало любую страницу, которая триггерила его lazy-загрузку (в т.ч. `/admin/messaging`)
- **E2E тесты (30 tests, 0 failures):** починены 11 падающих тестов — 429 rate-limiter (30→60), CSP фильтр, apiCall с JWT, admin token из storage state, feature flags route fix, legacy file селекторы, XSS тест через API+Profile page

### Документация обновлена
- **Оценка kimi.txt:** переписан с реальным статусом
- **AGENTS.md:** структурирован System Prompt (Core Rules, Code Quality, Data Rules, Git, Response Format). Добавлены: Project Structure, CI/CD Pipeline, Git Workflow, Testing Strategy, Security Rules, Production Deployment
- **AGENTS.md prompt templates:** добавлены 3 промта — функциональный аудит, аудит связки Админка-БД-Приложение, E2E-тест
- **AGENTS.md (июль 2026, 1-я итерация):** добавлены секции из Промты.txt — Итеративный подход (5 этапов), Redis + Backup, DB Migrations, расширенные Code Style и Security Rules
- **AGENTS.md (июль 2026, 2-я итерация):** добавлены 9 pitfall'ов (баланс скобок, сборка Vite, развод не баним, INTEREST_KEY_TO_ID и др.), 8 Past Mistakes (Register 500, Reports 500, chats.tsx SyntaxError и др.), 5 ключевых unit-тестов, 12 сценариев Playwright, 3 новых промпта (Unit-тесты, chats.tsx fix, settings-privacy fix), VITE_WS_URL/SMTP_FROM/VITE_API_URL в конфигурационную консистентность
- **persona.md:** исправлен порт API (3001 → 3002)
- **swagger-jsdoc:** установлен (отсутствовал, сервер не стартовал)
- **Playwright-тестирование:** `e2e/audit-full.spec.ts` — 11 сьюитов: health, registration, negative auth, like→match→chat (два браузера), admin flow, public pages, settings, chat, groups, негативные тесты, WS real-time
- **data-testid селекторы:** добавлены на login, register, forgot-password, settings, profile-edit, groups, search, search-filters, premium-success, admin-features, admin-users, admin-messaging, chats-chatId, chats.tsx, activity.tsx, contest.tsx
- **e2e helpers:** `audit.ts` (console/network/error tracker), `api.ts` (API calls + health check)
- **global-setup:** проверка `/health` + MySQL перед тестами

### Production Hardening
- **Логи:** Winston JSON во всех серверных файлах (0 console.log/error)
- **WebSocket:** pingInterval 10s, pingTimeout 5s, reconnect с exponential backoff (max 30s)
- **Error boundaries:** per-page для каждого lazy-роута (падение одной страницы не валит весь сайт)
- **Loading states:** добавлены в activity.tsx и chats.tsx (спиннер при загрузке)
- **AdMob:** динамический импорт `@capacitor-community/admob` v6.0.0, `cap sync android`
- **Frontend console.log:** обёрнуты в `import.meta.env.DEV`
- **noValidate:** добавлен на все формы авторизации (login, register, forgot/reset-password)
- **no-empty / unused expressions:** исправлены в admin-users.tsx, profile.tsx, chats.tsx
- **Seed data:** 50 пользователей (admin@mail.ru + user1-50@mail.ru), пароль demo123456; НОВЫЙ — user1 неактивен (is_active=0) для теста бана
- **Миграции:** исправлены 005_add_spatial_location.sql (DEFAULT для NULL location) и 006_add_soft_delete_audit.sql (удалён SELECT с несуществующей таблицы)
- **Auth fix:** регистрация теперь включает `location` в INSERT (была ошибка NOT NULL)
- **npm packages:** установлены `@capacitor/push-notifications`, `@socket.io/redis-adapter`, `mysql2` (root); в server — добавлен `"test": "vitest run"` скрипт
- **SMS интеграция в регистрацию:** auth.js + register.tsx — телефон сохраняется при регистрации
- **AI Moderation в upload:** moderateImage() при загрузке фото → moderation_status='flagged' при NSFW
- **FCM push:** fcm.js + push.js dual-канал (VAPID + FCM)
- **IAP scaffold:** server/src/routes/iap.js + миграция 008
- **Deep Links:** assetlinks.json + capacitor.config.ts + use-deep-links.ts
- **Ghost Mode (incognito):** `up.incognito = 0` в поиске, API GET/PUT `/api/settings/privacy`, премиум-гейтинг, синхронизация фронта с сервером
- **Passport Mode:** `passport_mode/city/lat/lng` в user_profiles, авто-использование passport_location в поиске, премиум-гейтинг, UI toggle + city input
- **Incognito в активности:** activity route фильтрует `up.incognito = 0` — скрыты визиты инкогнито-пользователей
- **GDPR Compliance:** `GET /api/data/export` (экспорт всех данных), `POST /api/data/erase/request` + `/confirm` (полное удаление с подтверждением), `POST /api/consent` + `GET /api/consent/history` (логирование согласий); миграция 010; UI кнопки в /settings/privacy
- **JWT_SECRET lazy getter:** middleware.js экспортирует `JWT_SECRET()` (функцию), а не константу. Тесты должны устанавливать `process.env.JWT_SECRET` ДО вызова `jwt.sign()`. Все 6 потребителей обновлены на `JWT_SECRET()`
- **BANNED_WORDS undefined в profile.tsx:** `BANNED_WORDS` был определён только в `profile-edit.tsx`, вызывал ReferenceError. Вынесен в `src/lib/constants.ts`, импортирован в оба файла
- **use-websocket.ts хардкод WS URL:** fallback `http://localhost:3002` заменён на `VITE_WS_URL` env / `window.location`
- **ErrorBoundary русский хардкод:** добавлены пропсы `fallbackTitle/Message/Button`, `SuspenseWrapper` передаёт `t()`
- **Дубликат /premium роута в App.tsx:** удалён дубликат внутри AppContainer; премиум-страницы только снаружи с SuspenseWrapper
- **search.tsx/"Все" vs "all":** фильтр города не работал — `search.tsx` проверял `"Все"`, а `search-filters.tsx` сохранял `"all"`. Обе стороны приведены к `"all"`
- **Хардкодные русские строки (i18n):** `profile.tsx`, `profile-edit.tsx`, `contest.tsx`, `photo-uploader.tsx`, `app-header.tsx` — все заменены на `t()`, добавлено ~40 новых translation keys
- **mail.js хардкод sender:** добавлен `EMAIL_FROM` env, warning в production
- **profile-edit.tsx DEFAULT_PROFILE:** функция `getDefaultProfile(t)` вместо внешней константы с русским текстом
- **premium-dialog.tsx ru-RU в formatPrice:** locale параметр + вызов с `language` из контекста
- **MySQL my.ini:** 2 невалидные опции (`loose-component_reference_cache`, `skip_component_reference_cache`) блокировали старт MySQL 8.4.3 — удалены
- **4 миграции (011-015):** `IF NOT EXISTS` для `ADD COLUMN` не работает в MySQL 8.4. Переписаны через `information_schema` + PREPARE. `INT`→`INT UNSIGNED` для FK (users.id — unsigned)
- **schedule.js:** TS-аннотации (`: any[]`, `: any`) в `.js` файле + named import `{ logger }` (logger.js экспортирует default) — исправлено
- **date-checkin.js:** `require()` в ESM-проекте — конвертирован в `import/export`
- **index.js:** `referralRoutes` не импортирован и не зарегистрирован; `dateCheckinRoutes` задублирован — исправлено
- **profile.js:** `GET /api/profile/me` отсутствовал после пулла; стоял после `/api/profile/:id` — Express матчил `me` как `:id`. Добавлен ПЕРЕД `:id`; POINT location удалён из ответа
- **email_verified_at:** снова появился в login SELECT после пулла — колонки нет в БД, убрана
- **vite.config.ts:** `rolldownOptions` — невалидная опция Vite 8 (должен быть `build.rollupOptions`); `@capacitor/push-notifications` и `@capacitor/geolocation` не установлены — пакеты установлены, конфиг исправлен

### Stage 1 — WebRTC Video/Voice Calls
- `server/src/ws.js`: +6 signalling событий (call-user, call-accepted, call-rejected, ice-candidate, end-call)
- `src/hooks/use-webrtc.ts`: новый — RTCPeerConnection + Google STUN + call states
- `src/components/video-call.tsx`: переписан — remote video, PiP, mute, fullscreen
- `src/components/voice-call.tsx`: переписан — audio call, mute, speaker toggle
- `src/pages/chats-chatId.tsx`: Phone/Video кнопки + WebRTC интеграция
- Сборка: ✅, тесты: ✅

### Stage 2 — Sharp Image Pipeline
- `server/src/image-pipeline.js`: новый — resize 800/400/200 + WebP (q80) + AVIF (q65)
- `server/src/routes/upload.js`: импортирует pipeline после успешного upload
- Сборка: ✅, тесты: ✅

### Stage 3 — Push Notifications (было готово до этапов)
- VAPID keys, Service Worker (`public/sw.js`), PwaRegistry, push API (GET/POST/DELETE /api/push/*)
- `subscribeToPush()` / `unsubscribeFromPush()` в настройках
- Интеграция с лайками (social.js), массовая рассылка из админки
- Unit-тесты: 6 тестов в push.test.js

### Stage 4 — SMS Verification (Twilio scaffold)
- `server/src/sms.js`: Twilio client (graceful mock без ключей)
- `server/src/routes/sms.js`: send-code/verify/status эндпоинты, rate-limit 3 SMS/час
- `database/migrations/004_add_sms_verification.sql`: таблица sms_verification + phone колонка
- twilio установлен в server/node_modules

### Stage 5 — AI Moderation (scaffold)
- `server/src/ai-moderation.js`: OpenAI Moderation + AWS Rekognition (graceful fallback), текстовая эвристика
- `server/src/routes/moderation.js`: check-text, check-image, review-profile, status эндпоинты
- Без ключей: текст → эвристика (regex), изображения → auto-approve

### Stage 6 — Push FCM Dual-Channel
- `server/src/fcm.js`: Firebase Cloud Messaging через HTTP API (graceful mock без ключа)
- `server/src/routes/push.js` (переписан): поддержка `platform` ('web'|'fcm'), dual-отправка (FCM + Web Push параллельно)
- `src/hooks/use-fcm-token.ts`: исправлен URL на `/api/push/subscribe` (был несуществующий `/api/push/fcm/register`)
- `src/hooks/use-push-capacitor.ts`: новый — Capacitor Push Notifications регистрация
- VAPID остаётся для веба, FCM для нативных APK

### Stage 7 — RevenueCat IAP
- `server/src/routes/iap.js`: webhook (INITIAL_PURCHASE/RENEWAL/CANCELLATION/EXPIRATION), GET /products, GET /status
- `database/migrations/008_add_iap.sql`: колонки provider, provider_subscription_id, status, current_period_end
- Зарегистрирован в index.js

### Stage 8 — Deep Links
- `public/.well-known/assetlinks.json`: Android App Links scaffold
- `capacitor.config.ts`: hostname, DeepLinks.enabled
- `src/hooks/use-deep-links.ts`: обработчик ссылок для Capacitor App

### Админка
- **admin/messaging:** страница работает (200, API GET/POST /api/admin/campaigns)
- **admin/analytics.js:** overview, retention, revenue-mix, registrations
- **PUT /features:** валидация пустого body → 400
- **Admin routes:** все покрыты тестами

### Тесты
- **173/179 тестов:** 49 фронт (12 файлов, 6 pre-existing localStorage mock failures в jsdom) + 124 сервер (12 файлов) + 30 E2E (2 spec-файла)

### Документация
- **server/.env.example:** все переменные с комментариями (Stripe, SMTP, Sentry, Redis, S3)
- **.env.example:** Sentry DSN, WS URL
- **README:** профессиональный список фич, секция backup
- **project-context.md:** полный контекст для AI-ассистентов

---
## Сессия «доделывание прод-версии» (этапы 0–6, август 2026)

### Этап 0 — базовые тесты + smoke
- Сервер 124/124 ✅, фронт 55/55 ✅ (полифилл localStorage в `src/test/setup.ts` для Node 25)
- Фиксы: геопоиск (радиус км→м + порядок params), GDPR export (`m.text`), сид групп (397 групп, 20 категорий) + `group_posts` таблицы (016)

### Этап 1 — Монетизация
- Tiers Plus/Gold/Platinum ✅, mock checkout ✅, лимит лайков 10/день ✅, feature flags ✅, админ-цены/статистика ✅
- Фикс: ads-config 500 (JSON-колонки mysql2) + миграция `017_add_config.sql`

### Этап 2 — Коммуникации
- Push-кампании + VAPID ✅, email-канал кампаний (раньше только вставлял запись — теперь шлёт через `mail.js`, пустой SMTP → лог «Would send»), SMS graceful без Twilio ✅

### Этап 3 — AI Icebreakers (было «не подключён» → ✅)
- Сид 8 тем / 40 вопросов (миграция `018_icebreakers_seed.sql`), endpoint `POST /api/icebreakers/suggest` (OpenAI если `OPENAI_API_KEY`, иначе случайные вопросы из БД, RU/EN), UI-чипы «Начни разговор» в пустом чате (`chats.tsx`), флаг `aiIcebreakers` в админке

### Этап 4 — A/B + Product Analytics (было ❌ → ✅)
- Миграция `019_experiments.sql` (experiments, experiment_assignments), роут `experiments.js` (assign по MD5-хэшу 50/50, track в `analytics_events`, админ CRUD)
- Хук `useExperiment.ts` + `useTrackEvent`, эксперимент `card_cta` в `search.tsx`, админ-страница `/admin/experiments`
- Трекинг: registration, like, match, premium_purchase

### Этап 5 — API Versioning
- `/api/v1/*` → `/api/*` + заголовок `X-API-Version: v1` (глобальный middleware, обратная совместимость)

### Этап 6 — Документация
- «Что сделано.txt» + AGENTS.md обновлены (записи 1–47)

### Багфиксы (август 2026)
- `messages.image_url` отсутствовал → миграция `020_messages_image_url.sql`
- `message_reactions` не существовала (чаты с сообщениями падали 500) → миграция `021_message_reactions.sql`
- `push_subscriptions` без колонки `platform` (пуши молча падали) → миграция `022_push_platform.sql`
- **«В Чаты иероглифы» — двойная кодировка сидов:** импорт SQL через `cmd <` БЕЗ `--default-character-set=utf8mb4` читал UTF-8 как CP866. Переимпортированы `icebreakers` (018), `chat_groups` (demo_groups.sql), `experiments` (019, через `scripts/fix-experiments.mjs`). Проверка: `scripts/scan-mojibake.mjs` — ALL CLEAN. Удалены мохибейк-сообщения и `?`-мусор тестов
- **GDPR consent flow:** чекбокс в регистрации (`register.tsx`, блокировка submit, `consent` в теле), запись `consent_log` в `auth.js`, тумблеры в настройках → `POST /api/consent`. Тесты: 126 сервер + 56 фронт
- **`users.verification_token/reset_token/reset_token_expires` не было в живой БД** — регистрация/сброс пароля падали 500 → миграция `023_auth_tokens.sql`
- **ResizeObserver полифилл** добавлен в `src/test/setup.ts` (Radix Checkbox требует его в jsdom)
- ВАЖНО: mysql-клиент без параметра кодировки на Windows читает файлы в системной кодировке (CP866); PowerShell inline `-Body`/`-d` с кириллицей → `?` в БД (тесты слать через файл + `curl --data-binary @file`)
- Демо-юзеры смещены на 1: admin = id 1, `userN@mail.ru` = id N+1

### Коммиты (август 2026)
`587966d` campaigns → `3102e40` icebreakers → `6442d1a` A/B+analytics → `62a41d4` versioning+020 → `8e45726` docs → `08b9181` UTF-8 re-seed + 021/022 + scripts

---
## Этап 11 (август 2026): Graceful degradation — работа без ключей
- Аудит всех внешних сервисов (Stripe/OpenAI/VAPID/FCM/SMTP/Twilio/Sentry/Redis/S3/RevenueCat/Bull): без ключей всё стартует, mock/fallback/skip + warn
- Исправлено: (1) `initQueues()` не вызывался — очереди (email/push/image) не работали даже с Redis, письма молча «терялись» → вызов в bootstrap index.js + closeQueues на SIGTERM; (2) пакет `openai` отсутствовал — OpenAI-ветки мёртвые → установлен ^7.5.0; (3) RevenueCat webhook без ключа принимал события без подписи → теперь 503 без ключа, 401 при неверной подписи (iap.js)
- Stripe: dev → mock-подписка 201; prod без ключа → 502 «not configured» (осознанно, чтобы не принимать оплату вслепую); STRIPE_LIVE=true без ключа → 500
- Тесты: сервер 126/126 ✅, E2E 44/44 ✅ (два прогона подряд), фронт 56/56 ✅

---
## Этап 10 (август 2026): E2E Video Date Scheduling + Safety Check-in
- `e2e/features.spec.ts` раздел 18, 2 теста: schedule CRUD (create/list/403 не-участнику/accept/decline/cancel/404 на повторный ответ) + check-in (contact, start, 400 на минуты вне 15-480, active, confirm, delete)
- 🐛 Реальный баг: `POST /api/schedule` — ISO-дата с T/Z падала 500 в MySQL 8.4 strict (`Incorrect datetime value`); фикс: `new Date(scheduled_at)` + 400 (schedule.js)
- ⚠️ Смещение демо-юзеров: `userN@mail.ru` = id N+1 (user4.json = id 5) — в тестах id берётся из payload JWT
- `check:ports` CI-friendly (без server/.env — инфо, не фейл); `npm run check:ports` добавлен в `deploy.yml`
- Тесты: E2E 44/44, сервер 126/126, фронт 56/56, build ✅

---
## Этап 9 (август 2026): Безопасность админ-API
- `adminAuth` (index.js + middleware/adminAuth.js) — активный: 401/403 вместо «пропустить всех»; публичен только `GET /api/admin/features` (его зовёт приложение без токена; с юзер-токеном тоже 200)
- `/api/admin/experiments*` (GET/POST/PUT/DELETE) были ВНЕ guard (роутер в корне index.js:217) — теперь каждый роут с `adminAuth`
- Проверки: PUT /features аноним → 401, GET /features аноним → 200, experiments с админ-токеном → 200. Тесты: 126+56+42 ✅
- ⚠️ Грабли: сервер запускать ТОЛЬКО из `server/` (`node src/index.js`) — иначе dotenv не подхватит `.env`, JWT_SECRET станет случайным → 401 везде. `check:ports` теперь фейлит без JWT_SECRET в server/.env

---
## Аудит «Оценка qwen.txt» (август 2026) — 9.8/10, рекомендации

- ✅ **Автоматизация pre-flight:** добавлен `npm run check:ports` (`scripts/check-ports.mjs`) — сверка портов (vite server 8081 / proxy 3002 / server .env и .env.example PORT / CORS_ORIGIN), warn на `console.log` в `server/src` (вне __tests__; сейчас только seed.js — легитимно). Может быть подключён к CI/pre-commit
- ✅ **E2E новых фич** — закрыто этапами 8+10 (features.spec.ts, 44/44, включая Video Date Scheduling и Safety Check-in)
- ✅ **6 pre-existing фронт-тестов** (localStorage в jsdom) — полифилл `ensureStorage` в `src/test/setup.ts` (этап 0), 56/56
- ⏳ **AGENTS.md >1500 строк** — пока 919; при превышении разделить на AGENTS-RULES/PITFALLS/CHECKLISTS
- ✅ **Stripe `express.raw`** — проверено: только `/api/premium/webhook` (`server/src/index.js:77`), не глобально

---
## Аудит «Оценка дипсик.txt» (август 2026) — проверено по факту

Проверено фактическое наличие всех файлов из списка DeepSeek (backend, БД, фронт, конфиги, тесты, инфраструктура) — **все присутствуют**, кроме:

- `server/src/emails/` — НЕ НУЖЕН: HTML-шаблоны писем инлайн в `mail.js`/`email.job.js` (reset/verify/кампании)
- `src/hooks/use-experiment.ts` / `use-track-event.ts` — есть как `useExperiment.ts` + `useTrackEvent` (camelCase, различие только в именовании)

Реальные пробелы из анализа:
- **E2E новых фич — ЗАКРЫТ (этапы 8+10, август 2026):** `e2e/features.spec.ts` (14 тестов): GDPR consent (регистрация→consent_log, UI-блокировка, тумблер настроек, revoke/restore, export, erase), Icebreakers (suggest + UI-чипы в пустом чате с включением флага aiIcebreakers), A/B (assign, track, админ-страница), Ghost Mode (premium gate), Profile Score, Video Date Scheduling (schedule CRUD/403/accept/decline/cancel) + Safety Check-in (contacts/start/confirm). Всего E2E: **44/44**. По пути исправлены баги: тень роута `PUT /api/profile/score` (за `:id`), 401 в settings.tsx/profile.tsx (нет Authorization), dev-login в auth-context.tsx/admin-guard.tsx перетирал сессию (теперь восстановление из JWT), токен только в sessionStorage (fallback localStorage), rate limiter 100→600 req/min, `POST /api/schedule` 500 на ISO-дату (MySQL strict). Детали: «Что сделано.txt», этапы 8, 10.
- **.env с реальными ключами** — единственное, что блокирует прод (список в `server/.env.example`)

---
## Что не сделано / ждёт — по оценке kimi

### 🔴 Критично (блокирует запуск) — код готов, ждут реальных ключей
| # | Задача | Статус | Что нужно |
|---|--------|--------|-----------|
| 1 | Push-уведомления FCM (Android) | ✅ Dual-канал (VAPID + FCM) в push.js, fcm.js, use-fcm-token.ts | Вписать FCM_SERVER_KEY |
| 2 | Deep Links (App Links + Universal Links) | ✅ assetlinks.json, capacitor.config.ts, use-deep-links.ts | Настроить домен + SHA256 fingerprint |
| 3 | IAP через RevenueCat (Apple/Google) | ✅ Webhook + роуты + миграция 008 | Создать RevenueCat проект, вписать webhook secret |
| 4 | SMS-верификация (Twilio) | ✅ Интегрирована в регистрацию (auth.js + register.tsx) | Вписать Twilio API-ключи |
| 5 | AI-модерация фото (AWS Rekognition) | ✅ Интегрирована в upload.js (NSFW → moderation_status) | Вписать AWS/OpenAI ключи |

### 🟠 Остаётся доделать
1. **Real API ключи:** вписать 10+ ключей в `server/.env`: Stripe, SMTP, Sentry, S3, Redis, FCM, RevenueCat, Twilio, AWS, OpenAI
2. **Deep Links:** настроить Universal Links (iOS) + App Links (Android)
3. **Background sync:** сообщения в offline → отправка при возвращении

### 🟡 Средний приоритет (конкуренция)
| # | Задача | Статус |
|---|--------|--------|
| 11 | AI Icebreakers (OpenAI) | ✅ Подключён к чатам (этап 3, август 2026) — suggest endpoint + чипы, fallback из БД без ключа |
| 12 | Profile Score → AI Recommendations | ✅ Score есть, AI рекомендации ждут ключа |
| 13 | Date Check-in (Safety) | ✅ Миграция 015, API роуты, страница /safety |
| 14 | A/B Testing Framework | ✅ Сделано (этап 4, август 2026) — experiments, assign 50/50, админка |
| 15 | Product Analytics (PostHog/Amplitude) | ✅ Сделано (этап 4, август 2026) — analytics_events + track по событиям |

### 🟢 Инфраструктура (месяц 2–3)
| # | Задача | Статус |
|---|--------|--------|
| 18 | CI/CD (GitHub Actions) | ✅ lint → frontend test → server test (MySQL) → E2E → deploy |
| 19 | Docker Compose (production stack) | ✅ app + nginx + Prometheus + Grafana |
| 20 | Redis Adapter для Socket.IO | ⚠️ Код есть в ws.js, требует REDIS_URL |
| 21 | Load Testing (k6) | ✅ ramp-up 10→100 users, 6 endpoints |
| 22 | Monitoring (Grafana + Prometheus) | ✅ HTTP/DB/WS/cache metrics, 7-panel dashboard |
| 23 | Backup Verification (восстановление) | ⚠️ Скрипт есть, проверки нет |

### 📋 Чеклист перед подачей в сторы
- [ ] 7 реальных API-ключей в server/.env (Stripe, SMTP, Sentry, S3, Redis, Twilio, AWS Rekognition)
- [ ] RevenueCat / IAP интегрирован
- [ ] Privacy Policy + Terms of Service
- [ ] Content Rating (17+)
- [ ] Screenshots (5+ для сторов)
- [ ] TestFlight / Internal Testing
- [ ] Age Verification (18+)

Детальное описание — в `Оценка kimi.txt`.

### Проверка страниц (июль 2026)
| Страница | Статус | Примечание |
|---|---|---|
| `/chats?groupId=102` | ✅ 200 | groupId обрабатывается на фронте (GroupFeed), API /api/chats игнорирует groupId |
| `/matches` | ✅ 200 | Новая страница, API /api/matches возвращает массив |
| `/premium` | ✅ 200 | Новая страница, API /api/premium/tiers работает |
| `/settings/privacy` | ✅ 200 | Использует localStorage (main settings.tsx так же) |
| `/profile/:id` | ✅ 200 | Редиректит на /user?id=:id |
| `/api/settings` | ❌ 404 | Роут не существует на бэкенде. **Починено:** settings-privacy.tsx переписан на localStorage |
| `/api/groups` (list all) | ❌ 404 | Роут не существует. Группы используют demo-data локально
| `/admin/messaging` | ✅ Починено | Падал с Vite SyntaxError из-за отсутствующего `</div>` в chats.tsx. Добавлен закрывающий тег |
| **Сборка** | ✅ `vite build` | 2.09s, 3580 modules, 0 errors |

### 🟢 Опционально (готово)
1. **Redis** — lazy singleton, `cache.js` с `cacheRoute()`/`cacheRoutePerUser()`/`invalidate()`. Кэш на profile GET (60s) + matches GET (30s). REDIS_URL активен в `server/.env`, Redis стартует через сервис Windows
2. **MySQL backup** — `scripts/backup-mysql.ps1` (авто-детект mysqldump, retention 7 дней). Task Scheduler создан: ежедневно в 03:00
3. **S3 scaffold** — `scripts/setup.ps1` — авто-копирование .env + генерация JWT_SECRET + npm install
4. **Документация из Промты.txt** — все 20 секций полностью перенесены в AGENTS.md. Добавлены: итеративный подход, 9 pitfall'ов, 20 past mistakes, 12 сценариев Playwright, 5 ключевых unit-тестов, 3 новых промпта, Redis+Backup, DB Migrations, расширенные Code Style и Security Rules, конфигурационная консистентность

---
## Ключевые файлы
| Файл | Назначение |
|---|---|
| `server/src/index.js` | Express entry — helmet, cors, rate-limit, /health, Sentry, WS, routes |
| `server/src/ws.js` | Socket.IO (pingInterval 10s, pingTimeout 5s) |
| `server/src/mail.js` | Nodemailer + retry (3 attempts) |
| `server/src/logger.js` | Winston JSON logger |
| `server/src/sentry.js` | Sentry init + beforeSend |
| `server/src/middleware/idempotency.js` | Idempotency-Key защита |
| `server/src/redis.js` | ioredis lazy client |
| `server/src/cache.js` | Redis cache middleware (cacheRoute, cacheRoutePerUser, invalidate) |
| `server/src/routes/admin/analytics.js` | analytics/overview, retention, revenue-mix, registrations |
| `server/src/fcm.js` | Firebase Cloud Messaging клиент (HTTP API + Admin SDK) |
| `server/src/routes/push-fcm.js` | FCM token registration API |
| `server/src/routes/iap.js` | RevenueCat IAP webhook + продукты |
| `server/src/routes/iap.js` | RevenueCat IAP webhook + продукты |
| `server/src/sms.js` | Twilio SMS клиент |
| `server/src/ai-moderation.js` | AI модерация (OpenAI + Rekognition) |
| `src/hooks/use-fcm-token.ts` | Capacitor Push Notifications регистрация |
| `src/hooks/use-deep-links.ts` | Deep Links обработчик |
| `database/migrations/migrate.js` | Миграции БД |
| `database/migrations/008_add_iap.sql` | IAP колонки в subscriptions |
| `database/migrations/009_add_ghost_passport.sql` | Ghost Mode + Passport Mode колонки |
| `database/migrations/010_add_gdpr.sql` | GDPR: consent_log + data_erase_requests |
| `server/src/routes/gdpr.js` | GDPR API (data export, erase, consent) |
| `server/src/routes/profile.js` | Профиль + GET/PUT `/api/settings/privacy` |
| `server/src/routes/schedule.js` | Video Date Scheduling API |
| `server/src/metrics.js` | Prometheus HTTP/DB/WS/cache metrics |
| `server/src/ws.js` | Socket.IO + TTL message cleanup + WS metrics |
| `nginx/swiftmatch.conf` | Production Nginx config |
| `.github/workflows/deploy.yml` | CI/CD pipeline |
| `monitoring/prometheus.yml` | Prometheus scrape config |
| `monitoring/grafana-dashboard.json` | Grafana dashboard (7 panels) |
| `k6/load-test.js` | Load testing script (10→100 users) |
| `project-context.md` | Полный контекст для AI-ассистентов |

## Production Readiness Summary
- **Тесты:** 173/179 — 49 фронт (12 файлов, 6 pre-existing localStorage mock failures в jsdom) + 124 сервер (12 файлов) + 30 E2E
- **Сборка:** `vite build` 2.09s, 3580 modules, 0 errors
- **CI/CD:** GitHub Actions — lint → frontend test → server test (MySQL) → E2E → deploy (SCP + pm2)
- **Monitoring:** Prometheus (`:9090`) + Grafana (`:3001`), HTTP/DB/WS/cache metrics, 7-panel dashboard
- **Docker Compose:** app + nginx + Prometheus + Grafana, named volumes
- **Load Testing:** k6 script, ramp-up 10→100 users over 60s
- **Логи:** Winston во всех серверных файлах (0 console.log/error)
- **Backup:** `scripts/backup-mysql.ps1` (auto-detect mysqldump, retention 7d) + Task Scheduler (ежедневно 03:00)
- **Setup:** `scripts/setup.ps1` — автоматическая настройка dev-окружения
- **.env.example:** оба файла содержат все переменные с комментариями
- **.env (root):** создан — VITE_WS_URL, VITE_API_URL, все VITE_* переменные для Vite dev-сервера
- **SEO:** sitemap.xml, расширенный robots.txt, улучшенный manifest.json
- **Error boundaries:** per-page (30+ lazy-роутов изолированы)
- **Loading states:** activity.tsx, chats.tsx, profile.tsx (спиннер)
- **Rate-limiter:** auth 60 req/min (поднят с 30 для E2E тестов)
- **E2E:** 30 tests, 0 failures, storageState (user2/админ/user4/user5), screenshot/video/trace на падение, аудит console errors
- **data-testid:** 15+ страниц покрыты селекторами для Playwright
- **Негативные тесты:** XSS, wrong password, empty form validation
- **WS real-time:** тест сообщения без перезагрузки + двухбраузерный тест
- **Unit-тесты:** use-websocket, language-context, feature-flags-context
- **Новые роуты:** /matches, /premium, /settings/privacy, /profile/:id, /schedule, /profile/:userId/score добавлены в App.tsx
- **Vite сборка:** 1.91s, 3582 modules, 0 errors (после фикса chats.tsx)

Для прода: вписать 10+ ключей в `server/.env`: Stripe, SMTP, Sentry, S3, Redis, FCM, RevenueCat, Twilio, AWS, OpenAI.

## Production Roadmap — что нужно добавить для продакшена

*(См. секцию «Что не сделано / ждёт — по оценке kimi» выше. Для актуального статуса.)*

Полное описание — в `Оценка kimi.txt`.

## Playwright E2E Test Coverage
- **44 tests, 0 failures** (~55s): `e2e/audit-full.spec.ts` (31) + `e2e/features.spec.ts` (13)
- **audit-full.spec.ts** — 12 сьюитов, все используют storageState:
  - `Health & Infrastructure` — API healthcheck, frontend loads
  - `Registration flow` — регистрация нового юзера через UI
  - `Negative auth tests` — wrong password, empty form validation
  - `Like → Match → Chat` — API лайки, проверка match, отправка сообщения (storageState user4)
  - `Admin flow` — login, toggle feature flags, search users (storageState admin)
  - `Public pages` — /, /login, /register, /forgot-password без ошибок
  - `Settings page` — toggle switches, logout button (storageState demo)
  - `Chat functionality` — open chat, message input visible (storageState demo)
  - `Groups page` — open create dialog (storageState demo)
  - `Negative & security` — XSS в bio, wrong password, empty email (storageState demo)
  - `WebSocket real-time` — сообщение без перезагрузки (storageState demo)
  - `WebSocket two-browser` — браузер A → браузер B через WS (storageState user4 + user5)
- **config:** screenshot/video on failure, json reporter, globalSetup создаёт .auth/*.json

## Данные для входа
| Email | Пароль | Роль |
|---|---|---|
| admin@mail.ru | demo123456 | Админ |
| user2@mail.ru | demo123456 | Обычный пользователь |
| user4..user50@mail.ru | demo123456 | 47 демо-пользователей |
| user1@mail.ru | demo123456 | Забанен (is_active=0) |

## Актуальный статус — этапы 27–34 (август 2026)

> Свежие детали: «Что сделано.txt» (этапы 30–34), «Что доделать.txt» (журнал этапов 30+), AGENTS.md (грабли #27–31).

- **Тесты: 245/245** — E2E 44/44 ✅, фронт 58/58 ✅, сервер 143/143 ✅; vite build ✅, lint 0 errors ✅
- **Этап 27 (CI/CD):** deploy.yml — миграции БД выполняются на сервере перед pm2 restart (атомарно, secrets DB_* не нужны)
- **Этап 29 (Redis):** локальный Redis 5.0.14 на 127.0.0.1:6379, REDIS_URL в server/.env, сервер держит пул
- **Этап 30:** починен 500 на /api/invites (дрейф колонок social.js ↔ таблица invites); GDPR-консент работает и покрыт E2E (#13 — регистрация блокируется без согласия, consent_log пишется)
- **Этап 31 (EXPLAIN-аудит SQL):** все запросы сверены со схемой; найдены и починены скрытые 500: iap.js webhook (`subscriptions.updated_at` не существовала) и location.js (`user_profiles.user_id` → `id`)
- **Этап 32 (Android):** toolchain установлен (SDK 35/36 + JDK 21), AGP 8.13.2; `@revenuecat/purchases-capacitor` подключён; **debug APK собран** (`android/app/build/outputs/apk/debug/app-debug.apk`, копия на рабочем столе); для прода — домен, релизная подпись, ключ RevenueCat
- **Этап 33 (JWT → httpOnly cookie):** сервер ставит `sm_token` (24h) + `sm_refresh` (7d, SameSite=Lax, Secure в prod) на login/register/refresh/dev-login; мидлвари читают Bearer ?? cookie (`server/src/cookies.js`); GET `/api/auth/me` — probe (всегда 200), POST `/api/auth/logout` чистит куку + refresh_tokens; фронт на web не пишет токен в JS-storage (XSS-safe), читает легаси-storage для storageState/старых сессий; нативные сборки работают через Bearer как раньше
- **Этап 34 (безопасность по аудиту kimi/qwen/дипсик):** refresh rotation + reuse detection (миграция 025: `family_id`/`revoked`, replay ротированного → отзыв всей семьи), POST `/api/auth/logout-all`, revoke всех сессий при reset-password, account lockout 5 неудач → 429/15 мин, ws.js принимает токен только из handshake.auth (query-param убран)
- **Этап 35 (schema-validation + слепые зоны):** scripts/schema-validate.mjs (mysql_schema.sql vs information_schema, 53 таблицы; в CI и на deploy ДО migrate.js); убраны 4 мёртвые колонки схемы; найден и починен реальный баг - migrate.js падал на 018 (Duplicate entry), миграции 018-023 теперь идемпотентны (INSERT IGNORE + information_schema/PREPARE); usesCleartextTraffic убран из release-манифеста Android (debug-only) + guard scripts/check-native-config.mjs в build:native и CI; backup retention 7->30 дней; rollback-plan.md: down-миграций нет; security.headers.test.js (7 тестов против живого сервера, skipIf). Счётчики: server 156/156, front 58/58, E2E 44/44 = 258/258
- **Этап 36:** RevenueCat SDK подключён (@revenuecat/purchases-capacitor 13.4.1), require() -> динамический import() в iap.ts (ESM-бандл на нативе); graceful no-op без ключа сохранён
- **Этап 37:** AGENTS.md Security Rules - ПРАВИЛО adminAuth: только active-check middleware, запрет пассивных next(), ручных проверок роли в хендлерах и обхода единого гейта

## ���������� ������ � ����� 38�41 (23 ������� 2026)

> �����������: ���� �������.txt� (����� 38�41), ���� ��������.txt�, docs/runbook-keys.md.

- **��������: 284/284** � E2E 44/44 ?, ����� 61/61 ? (13 ������), ������ 179/179 ? (18 ������); vite build ?, lint 0 errors ?, schema-validate 54 ������� ?
- **���� 38 (Admin TOTP 2FA):** �������� 026 (users.totp_secret/totp_enabled); server/src/totp.js �� otplib v13 (generateSecret/generateURI/generateSync/verifySync, epochTolerance=30); routes/totp-2fa.js setup/enable/disable ����� adminAuth; login ������� totp_code ��� ���������� 2FA (401 TOTP_REQUIRED/TOTP_INVALID + lockout 5 �������); UI src/components/admin/two-fa-settings.tsx � admin.tsx; i18n two_fa_* RU/EN; ������ pre-existing ������ i18n common.zodiac.* x12 (en). ����� ����� 8/8 (setup>enable>401 ��� ����>401 ����� ���>200 ������>disable)
- **���� 39 (webhook ��������������� + upload security):** �������� 027 webhook_events (UNIQUE provider+event_id); premium.js checkout.session.completed: INSERT IGNORE ����� ������ ���������� � replay ������� �� ������ ����� ��������; premium-webhook.test.js 5 ������ (mock mode/missing sig 400/bad sig 400/��������/replay); upload.test.js +5 security (non-image, no-file 400, unauth 401, path traversal > uuid, shell.php.jpg > .jpg)
- **���� 40 (rate-limit spec + i18n completeness):** �������� �������� �� index.js � middleware/limiters.js (apiLimiter 600/min, authLimiter 60/min + ������� makeAuthLimiter/makeApiLimiter ��� ������������� ������); ratelimit.test.js 3 ����� (60 �� > 61-� 429 Too many auth attempts; api 600 > 429; ���������� ����������); i18n.test.ts � ����� RU-EN ���������, �������� �������� (������� � ������� circadian x5 � ru); e2e/features.spec.ts icebreakers: �������� ���������� ��� ������ ���� 20..199 (������ ������: � demo ��� ���� ���� �� ����� pid 22..60, existing=false ����������)
- **���� 41:** docs/runbook-keys.md � ��� ����� ����� (server/.env vs �������� .env � VITE_*, ������� ��������� � �����), ������������� ������� ������� (����� ���� > .env > ������� API > �����), ��-������� JWT/SMTP/Stripe secret+webhook_secret (�������: ����� endpoint > .env+������� > ������� ������; ����� ����� 39 �������� �� ������)/Twilio/OpenAI/FCM/RevenueCat/S3/Sentry/Redis/VAPID; ������� ������ ������� ��� ������ (���. ALTER USER �� � �������� git log -p --all -- server/.env)
- **�������:** 34320b2 (���� 38), b08971b (���� 39), ff23f1a (���� 40), 2632106+a9d8a3a+b9ab518 (���� 41 + docs)

## ���� 42 (23 ������� 2026) � ������ ���������� ������� kimi/qwen/������

> ������ ��������� 23.08 (�������� ����� ����� 37, ������� 258 � ��� �������). ������ ������� ������� � ���� ��������.txt�, ������ ������� ���������� �������.

- **�������� � Redis fallback ������� ����� �������** (������� �������� kimi ������� ����): ��� ������������ REDIS_URL ioredis ������ Bull ������ ��������� "Connection is closed" (flushQueue). ����: queue.js ������ probe ����������� Redis (����������� lazyConnect-������ + ping) �� �������� ��������; ���������� > ������� ����������� graceful, mail.js ������ � fallback "Would send". ���� process-������ unhandledRejection/uncaughtException � index.js. ����� ����� � redis://invalid-host-xyz: health/login/me/chats = 200, ������ ����
- **__Host- ������� cookie � prod** (kimi 1.2): cookies.js ������������ ACCESS_COOKIE (__Host-sm_token)/REFRESH_COOKIE (__Host-sm_refresh); �������� index.js logout + auth.js refresh/me �� ����������; dev-����� ������� > E2E �� ���������
- **Cleanup revoked refresh-�������** (kimi 1.3): cleanup.js purgeRevokedRefreshTokens (DELETE WHERE revoked=1 AND created_at < NOW()-INTERVAL 30 DAY) + startRefreshTokenCleanup ��� � ����� (unref), ����� � index.js
- **RevenueCat webhook security unit** (������ #9): iap-webhook.test.js 6 ������ (503 ��� �������/401 ��������, ����� Bearer > 401 ��� ��������� � ��, INITIAL_PURCHASE INSERT provider=revenuecat, CANCELLATION/EXPIRATION, ����������� ��� ��� ��������, ������ �� > 500 ��� ������ �������)
- **�������������� ��� ����:** WS reconnect race (kimi 1.4) ������ ������������ � use-websocket.ts ���������� ����� �� deps [token], backoff 1s>30s; CORS ������� (kimi 1.6): prod fail-fast ������� CORS_ORIGIN, ������� ����� Bearer; deploy.yml �������� cd /app/swiftmatch ����� migrate.js (������ qwen)
- **����� �����:** cleanup.test.js (3), cookies.test.js (3), iap-webhook.test.js (6)
- **��������: server 191/191 (21 ����), front 61/61 (13 ������), E2E 44/44 = 296/296**; lint 0 errors; ������� 4b8703e (���� 42), 38db106 (docs)

## ����� 43-45 (23 ������� 2026)

- **43:** E2E security-blind-spots.spec.ts - GDPR cascade (erase->confirm->401/404), refresh race (������������ refresh ����� ������� -> 200+401 reuse; reuse �������� ����� ������� - �������� ����������), cookie edge cases. Rate-limit standardHeaders:true + ���� Retry-After
- **44:** playwright.config.ts ������� firefox/webkit �� CROSS_BROWSER=1 (16/16 passed); nginx ������������� (86400s ������ � /socket.io/); RTO-����� verify-backup.ps1 = 3.6 ��� -> rollback-plan.md N+1; single-instance rate-limit ������� -> N+2. �������� 299/299
- **45 (XSS alert("xss") � ���� � ����):** ����� ����� id=2 ����������� � bio ��� ����� (React ���������� - ���������� �� ����). sanitize.js ������: <script> � ����������, on*= ����������� ��� �����, javascript:/vbscript:/data:text/html URI, ����� alert/prompt/confirm ������ - �� ����������; "<3"/"a<b" �� ���������. �� �������. ����� �������� PUT ������� -> residue false. sanitize.test.js 7 ������
- �������: b8efe63 (43-44), 2c4df66 (docs), 23f802d (45)
- �������� ����: server 192/192 (22 �����) + front 61/61 + E2E 47/47 = 300/300
